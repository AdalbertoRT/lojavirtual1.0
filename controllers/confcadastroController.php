<?php
class confcadastroController extends controller{

    public function index(){
        // $string = explode("@", $email);
        // $string = explode(".", $string[1]);
        // $string = "http://www.".$string[0].".com";
        
        // $dados = array();

        // return $this->loadView("confcadastro", $dados);
    }
}